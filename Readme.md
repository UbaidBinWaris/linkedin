I have updated the bot to support both modes!

How to use:
Headless Mode (Default) - Runs in background:
bash
node index.js
Visible Mode - Opens the browser window:
bash
node index.js --visible
You can now verify the login process visually or run it silently as needed